# 에이전트 시스템 설계서: Portfolio Auto-Agent System (Multi-Depth Sync)

## 1. 작업 컨텍스트 문서 (Task Context)

### 1.1 작업 배경 (Background)
- 웹디자이너로서 지속적으로 발생하는 다량의 포트폴리오 작업물을 수동으로 분류, 업로드, 레이아웃 빌드하는 과정은 생산성을 저해함.
- 로컬 파일 시스템(`aylee_portfolio`) 및 GitHub 원격 저장소와 연동하여, 디자이너가 파일을 직관적으로 분류해 넣는 행위 자체만으로 다계층(대-중-소분류) 웹 메뉴와 반응형 웹사이트를 실시간으로 빌드 및 배포하는 자동화 시스템을 구축하고자 함.

### 1.2 목적 (Objectives)
- **다계층 관리 자동화**: 중첩 폴더 트리 구조를 자동으로 분석하여 대분류, 중분류, 소분류 웹 내비게이션을 완전 자동 생성 및 동기화함.
- **디자인 최적화**: 심플하고 세련된 미니멀리즘 2단 좌/우 레이아웃의 반응형 웹 아웃풋 출력.
- **최종 수익화 확장성**: 이 자동화 파이프라인 코어를 패키징하여, 대용량 작업물을 체계적으로 관리하고자 하는 타 프리랜서 및 디자이너를 대상으로 한 '폴더 연동형 다계층 포트폴리오 자동 제작 솔루션(SaaS)' 비즈니스 모델로 확장.

### 1.3 범위 (Scope)
- **다계층 폴더 감시**: `aylee_portfolio` 내 하위 디렉토리 생성/삭제 및 최대 3단계 깊이(Depth)의 중첩 폴더 구조 감지.
- **콘텐츠 분류 아키텍처**: 확장자 필터링을 통한 미디어 파일(갤러리 뷰) 및 텍스트 파일(블로그 뷰) 자동 분류.
- **프로필 동적 반영**: 루트 경로의 `profile.txt`를 실시간 파싱하여 메인페이지 우측 영역 UI 동적 업데이트.
- **정적 웹 빌드**: HTML/CSS(Tailwind) 기반 구조적 페이지 빌드 및 배포.

### 1.4 입출력 정의 (Input / Output)
- **Input**:
  - 시스템 루트: `aylee_portfolio`
  - 고정 카테고리 디렉토리(대분류): `1 websites`, `2 web contents`, `3 bx & print`, `4 career`, `5 Ai` (동적 폴더 추가 시 대분류 실시간 자동 확장)
  - 중첩 디렉토리(중/소분류): 대분류 폴더 내부 하위 폴더(중분류), 중분류 내부 하위 폴더(소분류)
  - 프로필 구성 파일: `profile.txt` (디자이너 프로필 정보 및 기술 스택 해시태그 기재)
  - 배경 자산: `mainbg/mainbg.jpg`
  - 허용 미디어 확장자: `.jpg`, `.png`, `.gif`, `.pdf`
  - 허용 텍스트 확장자: `.txt`
- **Output**:
  - 구조화된 반응형 포트폴리오 웹사이트 정적 소스 (`index.html`, 계층별/카테고리별 서브 정적 페이지 및 사이드바 내비게이션 DOM)

### 1.5 제약조건 (Constraints)
- **메인페이지 UI Spec**:
  - 스크롤이 배제된 일체형 2단 좌/우 레이아웃 구조.
  - 화이트 배경(BG) 및 블랙 폰트 기반의 심플하고 정제된 스타일링.
  - `mainbg/mainbg.jpg`를 최하단 레이어 전체 배경으로 선언하고 반응형(Cover)으로 무조건 고정 배치.
  - **왼쪽 영역**: 최소 폭 유지. [홈페이지 제목] 및 [카테고리] 링크 텍스트 리스트 세로 배치 (**볼드** 처리 필수). 하위에 중분류/소분류 존재 시 아코디언 혹은 드롭다운 형태로 세련되게 트리 UI 구성.
  - **오른쪽 영역**: `profile.txt` 기반 타이포그래피 UI 셋업 (이아영님 프로필 및 기술 스택 시각화).
- **서브페이지 UI Spec**:
  - **계층 구조 시각화**: 중분류 및 소분류 페이지 진입 시, 상단에 현재 위치를 나타내는 브레드크럼(Breadcrumb) UI 배치.
  - **갤러리 형태**: 허용 미디어 확장자 진입 시 가로 5개 그리드(Grid) 레이아웃 적용, 세로축 무제한 자동 스택. 파일명을 타이틀로 치환 적용하며, 디렉토리 내 파일 삭제 시 웹 DOM에서도 즉각 동기화 삭제.
  - **블로그 형태**: `.txt` 파일 진입 시 구동. 본문 렌더링을 지원하되 파일명을 기반으로 한 [제목]은 굵게, [내용]은 기본 폰트로 출력.
  - **예외 처리**: 지정된 확장자(`jpg, png, gif, pdf, txt`) 외 파일 및 폴더 구조는 프로세스에서 전면 무시. (예: 100MB 초과 대용량 PSD 파일 등은 빌드 대상에서 철저히 제외)
- **푸터 Spec**: 대분류 카테고리와 가로 정렬 라인을 일치시켜 좌측 하단 고정 배정 (`lee ah young  010.4788.1301`).

### 1.6 용어 정의 (Definitions)
- **Folder Watcher**: 파일 시스템의 I/O 이벤트를 후킹(Hooking)하는 실시간 이벤트 리스너 모듈.
- **Directory Tree Parser**: 중첩된 폴더 구조를 재귀적으로 탐색하여 JSON 트리 데이터로 변환하는 파서.
- **Static Page Generator**: 파싱 데이터를 UI 템플릿 컴포넌트와 결합하여 배포용 무상태 HTML을 생성하는 빌드 엔진.

---

## 2. 워크플로우 정의 (Workflow)

### 2.1 단계별 흐름도 및 상태 전이 (State Transition)
### 2.2 영역 구분 및 LLM 의사결정 단계
- **코드 처리 영역 (Rule-based)**
  - `folder_watcher.py`에 의한 파일 시스템 시스템 이벤트 감지.
  - `tree_parser.py`를 이용한 로컬 디렉토리 재귀 탐색 및 다계층(Depth 1~3) 구조의 JSON 데이터화 생성.
  - 갤러리 가로 5열 CSS Grid 주입 및 데이터 연동 삭제.
  - `profile.txt` 라인별 split을 통한 고정 데이터 로드.
- **LLM 판단 영역 (Cognitive)**
  - **블로그 텍스트 콘텐트 서식화 의사결정**: 블로그 뷰용 `txt` 파일 적재 시, LLM이 단순 텍스트 덩어리를 분석함. 작성자가 서술식으로 풀어쓴 맥락에서 가장 세련되고 가치 있는 핵심 요약 문장을 판별하여 [제목]으로 자동 도출하고, 본문의 단락(Paragraph) 구조를 분석하여 세련된 레이아웃 가독성을 위한 마크다운 혹은 HTML 태그(`font-bold`, `text-base`, `leading-relaxed`) 주입 형식을 스스로 판단 및 결정함.

### 2.3 성공 기준 및 예외 처리 (Exception Handling)
- **성공 기준**: 로컬/원격 소스 변경 완료 후 최대 3초 이내에 다계층 내비게이션 맵 및 결과물 템플릿 컴파일링이 오차 없이 완료됨.
- **검증 방법**: 빌드 최종 단계에서 HTML DOM Validator 스크립트를 구동하여 필수 엘리먼트(좌측 다계층 내비게이션 트리, 상단 브레드크럼, 우측 프로필 카드, 가로 5열 배치 클래스 존재 여부)의 구조적 보존성 자동 검증.
- **실패 시 처리 방식**:
  - **재시도 (Retry)**: 파일이 다른 프로그램에 의해 편집 중(File Lock)일 경우, 1초 타임아웃 간격으로 최대 3회 재시도 진입.
  - **스킵 (Skip)**: 파일 손상 혹은 0바이트 자산, 미지원 대용량 확장자 유입 시 시스템 크래시 방지를 위해 로그 기록 후 해당 자산만 패스.
  - **에스컬레이션 (Escalation)**: 계층 파싱 오류(예: 4단계 이상 중첩폴더 진입), 메인 배경 자산(`mainbg.jpg`) 손실, UI 전체 CSS 컴파일 에러 등 사이트 정지급 오류 발생 시, 빌드 파이프라인을 롤백(Rollback)하고 에이전트 콘솔에 적색 경고 메시지와 에러 로그 추적 데이터를 출력하여 사용자에게 에러 상황을 즉시 리포팅함.

---

## 3. 구현 스펙 (Implementation Specification)

### 3.1 파일 및 폴더 구조 (System Directory)
### 3.2 에이전트 구조 및 스킬 스크립트 명세
- **에이전트 구조**: 시스템 경량화와 처리 속도 향상을 위해 **단일 오케스트레이터 에이전트(`main_agent.py`)** 구조를 채택함. 마스터 에이전트가 상태 머신을 통제하며 하위 경량화 스크립트(스킬)들을 필요 시점에 맞춰 절차적으로 가동함.

| 파일명 | 역할 및 스킬 정의 | 트리거 조건 | 처리 방식 |
| :--- | :--- | :--- | :--- |
| `folder_watcher.py` | `aylee_portfolio/` 디렉토리 하위 모든 자산의 생성/수정/삭제 이벤트 모니터링 및 시그널 전송 | 시스템 상시 가동 | 스크립트 (코드) |
| `tree_parser.py` | 디렉토리를 깊이 탐색하여 대-중-소 계층 관계를 도출하고 템플릿용 트리 데이터 생성 | 폴더 구조 변경 감지 시 즉시 호출 | 스크립트 (코드) |
| `llm_parser.py` | 유입된 `.txt` 콘텐츠의 문맥을 읽어 세련된 UI 제목/본문 구성 레이아웃 의사결정 | 블로그용 텍스트 유입 시 즉시 호출 | **LLM 판단** |
| `html_generator.py` | 파싱된 다계층 데이터, 파일 정보, `profile.txt` 속성을 템플릿에 매핑하여 최종 반응형 웹사이트 빌드 | 데이터 파싱 완료 직후 호출 | 스크립트 (코드) |

### 3.3 주요 산출물 파일 형식
- `index.html` (메인 레이아웃 및 프로필 상시 뷰포트 고정 화면)
- `category_*.html` (서브 카테고리별 다계층 사이드바, 브레드크럼, 그리드 갤러리 및 블로그 결합형 정적 화면)
- `style.css` (Tailwind CSS 최적화를 거친 모노톤 미니멀리즘 스타일시트 파일)